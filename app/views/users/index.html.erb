<div class='row'>
  <div class='col-md-6 col-md-offset-3'>
    <h2 class='page-title'>アカウント一覧</h2>
    <% @users.each do |user|%>
      <div>
        <p><%= user.name %></p>
        <p><%= user.email%></p>
      </div>
    <% end %>
  </div>
</div>
